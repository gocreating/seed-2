export default (app, models) => {
  // app.get('/api/todos', (req, res) => {
  //   models.todo
  //     .findAll()
  //     .then(function(todos) {
  //       res.json(todos);
  //     });
  // });
}